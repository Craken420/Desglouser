Archivos que se encontraron en reportes mavi y se copiaron a la version 5000

let propiedadObj = Object.getOwnPropertyNames(extraccionVariablesOriginal[key][propiedadObjOriginal[key3]])
                        for (key5 in propiedadObj) {
                            console.log(propiedadObj[key5])
                        
                            let propiedadObj2 = Object.getOwnPropertyNames(extraccionVariables[key2][propiedadObjVariables[key4]])
                            for (key6 in propiedadObj) {
                                console.log(propiedadObj[key6])
                                if (propiedadObj[key5] == propiedadObj2[key6]) {
                                    console.log('propiedades iguales')
                                } else {}
                            }
                        }